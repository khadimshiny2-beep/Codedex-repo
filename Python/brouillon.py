# def calculer_stats(notes):
#     somme   = sum(notes)
#     moyenne = somme / len(notes)
#     minimum = min(notes)
#     maximum = max(notes)
#     return f"Somme: {somme}\nMoyenne: {moyenne}\nMinimum: {minimum}\nMaximum: {maximum}"


# liste = [12, 15, 14, 16, 10]

# l = calculer_stats(liste)

# # print(l)

# from random import choices

import numpy as np
def slot_machine(symbols):
    if choices(symbols, k=3) == ["7️⃣", "7️⃣", "7️⃣"]:
        return "JACKPOOOOT 🤑💰\n*insert coins flooding from machine animation*"
    return "Bayilène naar bi guene bègne voila 😂😂"


symbols = ["🍒", " 🍇", "🍉", " 7️⃣"]

roll = slot_machinesymbols()
print(roll)

m = np.array([],O[kf]
)